[pytest]
asyncio_mode = auto
env =
    PUSH_DATA_DIR=local_data
# NO asyncio_fixture_scope needed – 0.23 defaults to function scope
filterwarnings =
    ignore::pytest.PytestDeprecationWarning
    ignore::DeprecationWarning:fastapi.*
