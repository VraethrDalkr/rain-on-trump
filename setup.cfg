[metadata]
name = rain-on-trump
version = 0.3.1

[options]
package_dir =
    = backend
packages = find:
install_requires =
    httpx>=0.27

[options.extras_require]
test =
    pytest>=7.0
    pytest-asyncio>=0.23
    pytest-httpx>=0.23

[tool:pytest]
testpaths = backend/tests
